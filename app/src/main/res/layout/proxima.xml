<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:orientation="vertical"
    android:background="@drawable/caminoazul"
    android:padding="5sp"


    >
    

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Disponible en próximas versiones"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/pronto"
        app:layout_constraintBottom_toTopOf="@id/requiere_donacion"
        android:textAlignment="center"
        android:textSize="@dimen/texto"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/requieredonacion"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/requiere_donacion"
        app:layout_constraintTop_toBottomOf="@id/pronto"
        android:textAlignment="center"
        android:textSize="@dimen/texto"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
